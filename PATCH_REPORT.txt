Task: SANDBOX-00 - prepare GLM sandbox
Timestamp: 2026-05-09 20:17:44 +03:00

Sandbox path:
C:\Users\Den\Desktop\GLM_test\glm_game_sandbox

Production path status:
- Confirmed: production folder was not touched.
- Confirmed: no writes were made outside the sandbox folder.

Project root inspection:
- Present: AGENTS.md
- Present: README.md
- Present: index.html
- Present: src/
- Present: assets/
- Present: docs/
- Present: package.json
- Present: BAT helpers in repo root

Files changed:
- AGENTS.md
- README.md
- README_GLM_SANDBOX.md
- .gitignore
- 05_RUN_VISUAL_SCENARIOS.bat
- tools/visual_scenarios.mjs
- PATCH_REPORT.txt

Files disabled or renamed:
- 04_SYNC_WORK_MIRROR_TO_GOOGLE_DRIVE.bat -> 04_SYNC_WORK_MIRROR_TO_GOOGLE_DRIVE.DISABLED_IN_GLM_SANDBOX.bat
- tools/sync_work_mirror.py -> tools/sync_work_mirror.DISABLED_IN_GLM_SANDBOX.py
- tools/sync_visual_screenshots.py -> tools/sync_visual_screenshots.DISABLED_IN_GLM_SANDBOX.py

Folders removed from sandbox:
- node_modules/
- backup/
- _reports/
- test-results/
- _exports/
- _archive/

Folders ignored or kept out of git:
- node_modules/
- backup/
- _reports/
- test-results/
- _exports/
- _archive/
- .env
- *.log
- *.tmp
- *.bak
- screenshot/cache/temp artifacts via .gitignore
- _gpt_state/ intentionally left in working copy and not planned for staging
- _inbox/ intentionally left in working copy and not planned for staging

Dangerous reference audit:
- Executable mirror sync BAT was disabled by rename.
- Direct Python mirror sync helpers were disabled by rename.
- Visual screenshot flow was changed to stay local only.
- Remaining references still exist in docs, _gpt_state, _inbox, package-lock.json, and disabled files.
- Remaining references are informational or historical in this patch, not active sync entrypoints.

Git status summary:
- Git repository initialized in sandbox.
- Active branch set to sandbox/main.
- Before staging, repo contains untracked project files as expected for a fresh init.
- Plan for staging: exclude _gpt_state/ and _inbox/ from the initial clean sandbox commit.

GitHub status:
- GitHub CLI status check result: gh not found on this machine in current PATH.
- No token workaround was attempted.
- No remote was created automatically.

Checks run:
- node --check src/main.js -> PASS
- node --check tools/visual_scenarios.mjs -> PASS
- package.json inspected -> only devDependencies present, no npm scripts to run
- root audit re-run after cleanup -> active mirror writers disabled; remaining refs are mostly historical/docs/context

Risks:
- Some non-executable docs still mention production paths and mirror workflow.
- package-lock.json still contains legacy package name four_elements_core_base.
- _gpt_state/ and _inbox/ still contain historical context and production-path references if a future user chooses to stage them.
- Root BAT helpers 01/02 still mention production-era comments, though the dangerous sync launcher itself is disabled.

Exact next steps for user / GPT / GLM:
1. Review the initial sandbox commit on branch sandbox/main.
2. Add a private GitHub remote manually after gh is installed/authenticated, or use:
   git remote add origin <PRIVATE_REPO_URL>
   git push -u origin sandbox/main
3. Keep all future work on feature branches created from sandbox/main.
4. Do not re-enable Google Drive sync inside this sandbox.
5. If desired, do a follow-up cleanup patch to ignore or remove _gpt_state/ and _inbox/ before sharing the repo more broadly.

---

Task: GHPAGES-02 - Add GitHub Pages hardening and live demo link
Timestamp: 2026-05-10 18:07:00 +03:00
Branch: glm/ghpages-02-live-demo-nojekyll (from sandbox/main)

Purpose:
- Add .nojekyll to prevent GitHub Pages from ignoring folders starting with underscore or dot.
- Add a live demo link to README.md so visitors can launch the game directly.
- Record the change in PATCH_REPORT.txt.

Changed files:
- .nojekyll (new, empty)
- README.md (added live demo link block, kept sandbox warning and all existing rules)
- PATCH_REPORT.txt (appended this entry)

Gameplay code and assets confirmation:
- Confirmed: no gameplay code was modified.
- Confirmed: no asset files were touched.
- Confirmed: src/main.js was not modified.
- Confirmed: Google Drive sync remains disabled.

Checks run:
- node --check src/main.js -> PASS
- Visual diff review: only .nojekyll, README.md, PATCH_REPORT.txt changed.

Production path status:
- Confirmed: production folder was not touched.
- Confirmed: no writes were made outside the sandbox folder.

---

Задача: WORKFLOW-02 — Упростить процесс: Fast lane без PR для низкорисковых задач
Дата: 2026-05-10
Lane: Fast
Ветка: sandbox/main (direct push)

Цель:
- Ввести два режима работы: Fast lane (direct push) и Review lane (PR обязателен).
- Fast lane: для docs, prompts, PATCH_REPORT, .nojekyll, текстовых правок без логики — commit + push напрямую в sandbox/main, без PR.
- Review lane: для src/main.js, src/core, src/config, assets, gameplay, экономика, combat, pathfinding, save/load, многофайловых изменений, рискованных задач — feature branch + PR.

Изменённые файлы:
- README_GLM_SANDBOX.md (GitHub workflow: добавлены Fast lane и Review lane с правилами; обновлён forbidden list)
- AGENTS.md (правило 1 обновлено: Fast lane — direct push; правило 2: Review lane — PR; секция Git and review workflow обновлена)
- docs/prompts/PROMPT_NEW_CHAT_GPT_FOUR_ELEMENTS_GLM_FIRST.txt (GLM роль: Fast lane / Review lane; секция 3: выбор lane; секция 4: разделение задач по lane; секция 6: обновлённые hard rules; секция 7: merge checklist только Review lane)
- docs/prompts/PROMPT_NEW_CHAT_GLM_FOUR_ELEMENTS_SANDBOX.txt (секция 1: Two lanes; секция 2: обновлённые hard rules; секция 3: Fast lane / Review lane branch rules; секция 5: PATCH_REPORT с lane; секция 8: final response с lane и commit hash)
- PATCH_REPORT.txt (добавлена эта запись)

Что зафиксировано:
1. Fast lane — direct push в sandbox/main для низкорисковых задач (docs, prompts, текст).
2. Review lane — feature branch + PR для рискованных задач (код, ассеты, gameplay).
3. PR не обязателен для Fast lane.
4. Если не уверены — default Review lane.
5. PATCH_REPORT и final report теперь содержат lane и commit hash.

Выполненные проверки:
- node --check src/main.js -> PASS
- Визуальная проверка diff: изменены только документация и промпты, код и ассеты не затронуты.

Подтверждение безопасности:
- Игровой код не изменён.
- Ассеты не затронуты.
- src/main.js не тронут.
- Google Drive sync не включён.
- Production paths не затронуты.

Примечание:
- PR не создавался, так как это Fast lane задача (docs/prompts only).

---

Задача: PATCH-SCOUT-01 — подключить scout unit shell
Дата: 2026-05-10
Lane: Review
Ветка: glm/patch-scout-01-unit-shell (от sandbox/main)

Цель:
- Подключить scout как минимальный unit shell: код игры знает тип scout, грузит и отрисовывает его 8-direction idle sprites.
- НЕ полноценная механика разведчика — только shell.

Изменённые файлы:
- src/config/units.js — добавлен scout в FE_UNITS: name, hp, speed, view, canAttack:false
- src/config/sprite_profiles.js — добавлен scout в FE_SPRITE_PROFILES.units: footprint, size, anchor, alphaCutoff
- src/core/asset_loader.js — добавлен scout в units (fallback image) и unitAnimations (8-dir idle через loadUnitAnimation)
- src/main.js — добавлен scout в 4 списка: spawnBuilderDust, updateBuilderDust, getCalibratedUnit (dev hotkeys), updateBuilderDust вызов в game loop

Параметры scout:
- name: 'Разведчик'
- hp: 70
- speed: 0.72 (выше light_tank 0.55 и harvester 0.46, но без балансировки)
- view: 7
- cargo: 0
- canAttack: false
- costElement: 1, productionTime: 18

Путь к ассетам:
- assets/factions/<faction>/units/scout_8dirs/scout_idle_dir<d>_0.png
- 8 направлений, idle-only, move frames = idle (как harvester и light_tank)

Что НЕ сделано специально:
- Scout НЕ добавлен в производство (factory UI / units_factory queue) — это требует отдельного патча с UI/экономической логикой.
- Scout НЕ добавлен в factoryCanAffordAnyUnit — не производится.
- Scout НЕ добавлен в combat / attack систему — canAttack:false.
- AI / bot scouting НЕ добавлен.
- Pathfinding НЕ изменён — scout использует стандартный unit_controller.
- Save/load НЕ изменён — createUnit автоматически создаёт scout с корректными полями.

Выполненные проверки:
- node --check src/main.js -> PASS
- node --check src/config/units.js -> PASS
- node --check src/config/sprite_profiles.js -> PASS
- node --check src/core/asset_loader.js -> PASS
- node --check src/core/unit_controller.js -> PASS
- Статическая проверка scout ассетов: 32 файла (4 фракции × 8 направлений) — все на месте

Подтверждение безопасности:
- Ассеты не изменены: да (ассеты были добавлены ранее в ASSET-SCOUT-01)
- src/main.js изменён: да (4 минимальных правки — добавление scout в списки dust/dev)
- gameplay изменён: нет
- economy изменена: нет
- combat изменён: нет
- pathfinding изменён: нет
- Google Drive sync тронут: нет
- production paths тронуты: нет

---

Задача: PATCH-SCOUT-01B — исправить способ dev-теста scout через консоль
Дата: 2026-05-10
Lane: Review
Ветка: glm/patch-scout-01b-dev-spawn (от glm/patch-scout-01-unit-shell)

Проблема:
В отчёте PATCH-SCOUT-01 была указана команда для теста scout из консоли:
  createUnit('scout', 10, 10)
Но createUnit — внутренняя функция IIFE в main.js, не экспортирована в window.
Команда падала с: Uncaught ReferenceError: createUnit is not defined

Цель:
Добавить безопасный dev-only способ создать scout (и любой другой юнит) из браузерной консоли.

Изменённые файлы:
- src/main.js — добавлена функция window.FE_DEV_SPAWN_UNIT рядом с FE_EXPORT_SNAPSHOT

Как работает FE_DEV_SPAWN_UNIT(type, x, y):
- Проверяет, что игра запущена (game.screen === 'game')
- Проверяет, что тип юнита существует в UNIT_DEFS
- Если x/y не указаны — спавнит рядом с hq_base игрока
- Проверяет границы карты
- Создаёт юнит через внутренний createUnit и добавляет в game.units
- Пишет понятный console.warn с именем, id и координатами
- Возвращает созданный unit или null при ошибке

Новая команда теста:
  FE_DEV_SPAWN_UNIT('scout', 10, 10)
  FE_DEV_SPAWN_UNIT('scout')           — рядом с HQ
  FE_DEV_SPAWN_UNIT('light_tank', 5, 5) — работает для любого типа

Что НЕ сделано специально:
- createUnit НЕ экспортирован напрямую — доступ только через FE_DEV_SPAWN_UNIT
- scout НЕ добавлен в производство/factory
- combat НЕ изменён
- pathfinding НЕ изменён
- economy НЕ изменена

Выполненные проверки:
- node --check src/main.js -> PASS
- Статическая проверка: window.FE_DEV_SPAWN_UNIT добавлен в main.js (строка 11864)

Безопасность:
- assets изменены: нет
- economy изменена: нет
- combat изменён: нет
- pathfinding изменён: нет
- production/factory изменён: нет
- Google Drive sync тронут: нет

---

Задача: PATCH-SCOUT-01C — исправить визуальную посадку scout и управление движением
Дата: 2026-05-10
Lane: Review
Ветка: glm/patch-scout-01c-visual-control (от sandbox/main)

Проблема:
1. Scout визуально слишком крупный (size:[92,92]), плохо посажен на клетке.
2. HP bar перекрывает корпус, selection ring слишком низко.
3. Scout не едет по команде движения — не получает updateUnitMovement в game loop.
4. Scout не имеет собственного dir map / dir offsets — использует builder как фоллбэк.
5. Scout отсутствует в dust-параметрах, click marker types, selection ring tune.

Что исправлено:

1. Visual profile (sprite_profiles.js):
   - size: [92,92] → [80,80] — scout меньше танка, но крупнее мелких юнитов
   - hpOffset: 44 → 34 — HP bar выше корпуса

2. HP bar позиция (main.js):
   - Добавлен scout в цепочку hpY расчёта: anchorScreenY - 52 * z
   - HP bar теперь над корпусом, не перекрывает его

3. Selection ring (main.js):
   - Добавлен scout в ringTuneByType: { x:0, y:-16, rx:0.88, ry:0.84 }
   - Ring под юнитом, компактнее чем у builder

4. Direction map + offsets (runtime_flags.js + main.js):
   - Добавлен FE_SCOUT_DIR_MAP = [2,0,0,6,6,4,4,2]
   - Добавлен FE_SCOUT_DIR_OFFSETS (8 направлений, начальные нули)
   - Добавлен FE_SCOUT_FORCE_DIR = null
   - main.js: scout включён в цепочку dirMap и dirOffsets

5. Movement (main.js):
   - Добавлена ветка в game loop: if (u.type==='scout') updateUnitMovement(u,dt);
   - Scout теперь двигается по команде через стандартный movement pipeline

6. Dust (runtime_flags.js + main.js):
   - FE_SCOUT_DUST_WHEEL_Y, RADIUS_MULT, ALPHA_MULT, TRAIL_COUNT, BURST_COUNT
   - Scout включён в цепочку wheelYOffsetFlag, defaultWheelYOffset, radiusMult, alphaMult

7. Click marker (runtime_flags.js):
   - Scout добавлен в FE_UNIT_CLICK_MARKER_TYPES

8. Unit menu (main.js):
   - Добавлен scout в openUnitMenu с кнопками "Стоп" и "Отмена"

9. Dev hotkeys (main.js):
   - Scout включён в getDirOffsetsForUnit, getForceDirForUnit, setForceDirForUnit

Изменённые файлы:
- src/config/sprite_profiles.js — size, hpOffset для scout
- src/config/runtime_flags.js — FE_SCOUT_DIR_MAP, DIR_OFFSETS, FORCE_DIR, dust params, click marker
- src/main.js — dirMap, dirOffsets, hpY, selection ring, movement loop, dust params, unit menu, dev hotkeys
- PATCH_REPORT.txt

Выполненные проверки:
- node --check src/main.js -> PASS
- node --check src/config/sprite_profiles.js -> PASS
- node --check src/config/runtime_flags.js -> PASS
- node --check src/config/units.js -> PASS
- node --check src/core/asset_loader.js -> PASS

Как тестировать:
1. Запустить игру
2. Открыть консоль: FE_DEV_SPAWN_UNIT('scout')
3. Кликнуть на scout — выделяется, видны ring и HP bar
4. Кликнуть на свободную клетку — scout едет
5. Контекстное меню: "Разведчик" / "Стоп" / "Отмена"

Что НЕ сделано специально:
- Scout НЕ добавлен в производство (factory UI)
- Combat НЕ изменён — scout с canAttack:false
- AI / bot scouting НЕ добавлен
- Pathfinding НЕ изменён — scout использует стандартный BFS
- Save/load НЕ изменён
- Ассеты НЕ изменены

Безопасность:
- assets изменены: нет
- economy изменена: нет
- combat изменён: нет
- pathfinding изменён: нет
- production/factory изменён: нет
- Google Drive sync тронут: нет

---

Задача: PATCH-SCOUT-01D — исправить orientation / dir mapping scout
Дата: 2026-05-10
Lane: Review
Ветка: glm/patch-scout-01d-dir-mapping (от sandbox/main)

Проблема:
Scout едет боком — визуально смотрит не в сторону фактического движения.
При движении south-west показывает sprite, который смотрит north-east, и т.д.

Причина:
FE_SCOUT_DIR_MAP был скопирован с harvester/light_tank: [2,0,0,6,6,4,4,2].
Но scout sprites используют другую ориентацию: sprite dirN визуально смотрит
в направлении (N+1)%8, т.е. весь набор повёрнут на +1 step CW относительно
стандартной 8-dir конвенции (dir0=east, dir2=south, dir4=west, dir6=north).

Старый DIR_MAP давал:
- rawDir=3 (SW) → sprite=6 (смотрит NE) — юнит едет боком/назад
- rawDir=7 (NE) → sprite=2 (смотрит S)  — юнит едет боком

Что исправлено:

1. FE_SCOUT_DIR_MAP (runtime_flags.js):
   - Старый: [2, 0, 0, 6, 6, 4, 4, 2] (копия harvester)
   - Новый:  [7, 0, 1, 2, 3, 4, 5, 6]
   - Формула: DIR_MAP[rawDir] = (rawDir + 7) % 8
   - Компенсирует поворот спрайтов на +1 CW

2. unitDir8 forceDir (main.js):
   - Добавлен scout и light_tank в цепочку forceDir внутри unitDir8()
   - Раньше forceDir применялся только к harvester и builder
   - Теперь FE_SCOUT_FORCE_DIR и FE_LIGHT_TANK_FORCE_DIR работают корректно
     при dev-калибровке через горячие клавиши

Проверка новой DIR_MAP:
- rawDir=0 (E)  → sprite=7 → faces (7+1)%8=0=E  ✓
- rawDir=1 (SE) → sprite=0 → faces (0+1)%8=1=SE ✓
- rawDir=2 (S)  → sprite=1 → faces (1+1)%8=2=S  ✓
- rawDir=3 (SW) → sprite=2 → faces (2+1)%8=3=SW ✓
- rawDir=4 (W)  → sprite=3 → faces (3+1)%8=4=W  ✓
- rawDir=5 (NW) → sprite=4 → faces (4+1)%8=5=NW ✓
- rawDir=6 (N)  → sprite=5 → faces (5+1)%8=6=N  ✓
- rawDir=7 (NE) → sprite=6 → faces (6+1)%8=7=NE ✓

Изменённые файлы:
- src/config/runtime_flags.js — FE_SCOUT_DIR_MAP обновлён
- src/main.js — forceDir в unitDir8: добавлены scout и light_tank
- PATCH_REPORT.txt

Выполненные проверки:
- node --check src/main.js -> PASS
- node --check src/config/runtime_flags.js -> PASS

Безопасность:
- assets изменены: нет
- economy изменена: нет
- combat изменён: нет
- pathfinding изменён: нет
- production/factory изменён: нет
- size / hpOffset / selection ring: не тронуты
- angle->dir логика: не изменена (rawDir расчёт тот же)
- Google Drive sync тронут: нет

---

Задача: PATCH-SCOUT-01E — final visual tuning scout
Дата: 2026-05-10
Lane: Review
Ветка: glm/patch-scout-01e-visual-tuning (от sandbox/main)

Проблема:
1. FE_SCOUT_DIR_MAP [7,0,1,2,3,4,5,6] из 01D не соответствует фактической ручной
   dev-калибровке — scout всё ещё смотрит мимо направления движения.
2. Scout смещён относительно центра клетки / изометрического ромба.
3. Scout слишком мелкий при size=[80,80] — выглядит меньше, чем ожидается
   для видимого разведчика.

Что исправлено:

1. FE_SCOUT_DIR_MAP (runtime_flags.js):
   - Старый: [7, 0, 1, 2, 3, 4, 5, 6] (VLM-оценка из 01D)
   - Новый:  [3, 2, 1, 0, 7, 6, 5, 4] (подтверждён ручной dev-калибровкой)
   - Карта отражает реальную ориентацию scout sprite frames:
     rawDir 0(E)→sprite3, 1(SE)→2, 2(S)→1, 3(SW)→0,
     4(W)→7, 5(NW)→6, 6(N)→5, 7(NE)→4

2. Scout size (sprite_profiles.js):
   - Старый: size: [80, 80]
   - Новый:  size: [84, 84]
   - Увеличение на 4px — scout чуть крупнее, но остаётся меньше builder (92)
     и значительно меньше light_tank (104)

3. Scout centering (runtime_flags.js):
   - FE_SCOUT_DIR_OFFSETS обновлены с нулевых значений
   - Чётные направления (0,2,4,6): y=2 — кардинальные оси чуть ниже
   - Нечётные направления (1,3,5,7): y=1 — диагональные оси
   - Смещения выравнивают визуальную посадку scout по центру ромба

Изменённые файлы:
- src/config/runtime_flags.js — FE_SCOUT_DIR_MAP, FE_SCOUT_DIR_OFFSETS
- src/config/sprite_profiles.js — size для scout
- PATCH_REPORT.txt

Выполненные проверки:
- node --check src/config/runtime_flags.js -> PASS
- node --check src/config/sprite_profiles.js -> PASS

Безопасность:
- assets изменены: нет
- economy изменена: нет
- combat изменён: нет
- pathfinding изменён: нет
- production/factory изменён: нет
- movement logic не затронута
- angle→rawDir логика не изменена
- Google Drive sync тронут: нет

---

Задача: PATCH-SCOUT-01F — точная центровка scout по фото
Дата: 2026-05-10
Lane: Review
Ветка: glm/patch-scout-01f-centering (от sandbox/main)

Проблема:
После 01E scout всё ещё расположен слишком высоко относительно ромба клетки.
Смещение y=1..2 px было недостаточным — требуется ~20 px вниз.

Фактический замер по референс-скрину (dev-overlay):
- dir 2 целевой offset: {"x":-1,"y":22}
- dir 2 текущий (01E):   {"x":0, "y":2}
- Коррекция: x 0→-1, y 2→22 (≈+20 px вниз, -1 px влево)

Что исправлено:

1. FE_SCOUT_DIR_OFFSETS (runtime_flags.js):
   Старые offsets (01E):
     0: {x:0, y:2}   1: {x:0, y:1}
     2: {x:0, y:2}   3: {x:0, y:1}
     4: {x:0, y:2}   5: {x:0, y:1}
     6: {x:0, y:2}   7: {x:0, y:1}
   
   Новые offsets (01F):
     0: {x:-1, y:22}  1: {x:-1, y:21}
     2: {x:-1, y:22}  3: {x:-1, y:21}
     4: {x:-1, y:22}  5: {x:-1, y:21}
     6: {x:-1, y:22}  7: {x:-1, y:21}
   
   dir 2 выставлен точно по фото: {"x":-1,"y":22}
   Остальные направления — та же базовая коррекция (+20 px Y, -1 px X)

2. FE_SCOUT_DIR_MAP — НЕ менялся (рабочий [3,2,1,0,7,6,5,4] из 01E)

Изменённые файлы:
- src/config/runtime_flags.js — FE_SCOUT_DIR_OFFSETS
- PATCH_REPORT.txt

Выполненные проверки:
- node --check src/config/runtime_flags.js -> PASS

Безопасность:
- assets изменены: нет
- FE_SCOUT_DIR_MAP не менялся
- size не менялся ([84,84])
- movement logic не затронута
- angle→rawDir логика не изменена
- economy / combat / factory не затронуты
- Google Drive sync тронут: нет

---

Задача: PATCH-SCOUT-01G — поправить selection ring и убрать меню scout
Дата: 2026-05-10
Lane: Review
Ветка: glm/patch-scout-01g-ring-menu (от sandbox/main)

Проблема:
1. Selection ring у scout расположен слишком низко — заметно ниже юнита.
2. При клике на scout открывается меню "Разведчик / Стоп / Отмена",
   тогда как scout должен вести себя как light_tank: выделяется, показывает
   ring и HP, получает команды движения, но НЕ открывает отдельное меню.

Что исправлено:

1. Selection ring (main.js, ringTuneByType):
   - Старый: scout: { x: 0, y: -16, rx: 0.88, ry: 0.84 }
   - Новый:  scout: { x: 0, y: -38, rx: 0.88, ry: 0.84 }
   - Ring поднят на 22 px вверх (y: -16 → -38), теперь под корпусом/колёсами

2. Scout menu убран (main.js, openUnitMenu):
   - Раньше: scout имел свой else-if блок с setContextAt "Разведчик / Стоп / Отмена"
   - Теперь: scout добавлен в early-return рядом с light_tank
   - Код: if (u.type==='light_tank' || u.type==='scout') { updateSelectedInfo(); return; }
   - Блок else-if для scout удалён
   - Поведение выбора scout теперь аналогично light_tank

Не менялись:
- FE_SCOUT_DIR_MAP — не тронут
- FE_SCOUT_DIR_OFFSETS — не тронуты
- scout size [84,84] — не тронут
- movement logic — не затронута
- angle→rawDir — не изменена

Изменённые файлы:
- src/main.js — ringTuneByType.scout.y, openUnitMenu scout early-return
- PATCH_REPORT.txt

Выполненные проверки:
- node --check src/main.js -> PASS

Безопасность:
- assets изменены: нет
- economy изменена: нет
- combat изменён: нет
- pathfinding изменён: нет
- production/factory изменён: нет
- Google Drive sync тронут: нет

---

Задача: PATCH-SCOUT-02 — добавить scout в производство units_factory
Дата: 2026-05-10
Lane: Review
Ветка: glm/patch-scout-02-factory-production (от sandbox/main)

Цель:
Scout должен производиться через units_factory UI/queue, а не только через FE_DEV_SPAWN_UNIT.

Что сделано:

1. Factory production menu (main.js, openFactoryMenu, строка ~9702):
   - Старый: ['builder','harvester','light_tank']
   - Новый:  ['builder','harvester','light_tank','scout']
   - В UI фабрики появилась кнопка «Разведчик» с ценой и временем производства

2. Factory affordability check (main.js, factoryCanAffordAnyUnit, строка ~2335):
   - Старый: ['builder', 'harvester', 'light_tank']
   - Новый:  ['builder', 'harvester', 'light_tank', 'scout']
   - Factory indicator корректно показывает доступность scout при наличии 1 элемента

3. Production speed (main.js, productionSpeedForUnit, строка ~2442):
   - Scout добавлен в civilianProductionSpeed (как builder и harvester)
   - Старый: if (type === 'harvester' || type === 'builder')
   - Новый:  if (type === 'harvester' || type === 'builder' || type === 'scout')
   - Scout — лёгкий невоенный юнит, производящийся по гражданской скорости

Стоимость scout (уже задана в units.js):
- costElement: 1 (1 элемент фракции)
- productionTime: 18 сек
- Спавн: через существующую логику findSpawnCellNearBuilding → createUnit

Не менялись:
- FE_SCOUT_DIR_MAP — не тронут
- FE_SCOUT_DIR_OFFSETS — не тронуты
- scout size [84,84] — не тронут
- scout sprite profile — не тронут
- selection ring — не тронут
- movement logic — не затронута
- angle→rawDir — не изменена
- pathfinding — не затронут
- combat — не добавлен (canAttack:false)
- AI — не добавлен
- assets — не изменены
- economy core — не изменён (использована существующая логика)

Изменённые файлы:
- src/main.js — factoryCanAffordAnyUnit, openFactoryMenu, productionSpeedForUnit
- PATCH_REPORT.txt

Выполненные проверки:
- node --check src/main.js -> PASS

Безопасность:
- assets изменены: нет
- combat добавлен: нет
- AI добавлен: нет
- pathfinding изменён: нет
- economy core изменён: нет (использована существующая схема)
- Google Drive sync тронут: нет

---

Задача: GLM-UI-01 — обновить текст демо-сборки в главном меню
Дата: 2026-05-10
Lane: Review
Ветка: glm/ui-01-main-menu-demo-text-v2 (от sandbox/main)

Цель:
Заменить устаревший технический текст в главном меню на пользовательский
демо-сборочный текст. Главное меню показывает «Core Economy + Territory Fix v0.4»
и описание v0.4 — это разработческие строки, не предназначенные для публичной
демо-сборки на GitHub Pages.

Что сделано:

1. Subtitle (index.html, class="subtitle"):
   - Старый: Core Economy + Territory Fix v0.4
   - Новый:  Публичная демо-сборка · GitHub Pages

2. Small note (index.html, class="small-note"):
   - Старый: v0.4: сырьё, энергия, элементы, медленная территория, лимиты, HUD и сохранения.
   - Новый:  Демо-сборка: экономика, территория, юниты и сохранения в разработке.

Изменённые файлы:
- index.html — 2 строки заменены (subtitle + small-note)
- PATCH_REPORT.txt — добавлена эта запись

Не менялись:
- src/main.js — не тронут
- src/config/* — не тронуты
- src/core/* — не тронуты
- assets — не изменены
- gameplay / economy / combat — не затронуты
- Оставшиеся упоминания v0.4 в JS-комментариях и <title> — не тронуты

Выполненные проверки:
- node --check src/main.js -> PASS
- Визуальная проверка diff: только 2 строки в index.html + PATCH_REPORT.txt

Безопасность:
- Игровой код не изменён
- Ассеты не затронуты
- Google Drive sync не включён
- Production paths не затронуты

---

Задача: GLM-STRATEGY-01 — полный стратегический аудит проекта
Дата: 2026-05-10
Lane: Fast
Ветка: sandbox/main (direct push)

Цель:
Широкий стратегический аудит проекта Four Elements Remake с анализом
всех ключевых направлений и предложениями по развитию.
Это НЕ патч — код, ассеты и gameplay НЕ менялись.

Что сделано:
1. Прочитаны все проектные файлы: README, AGENTS, configs, docs/project/*, src/main.js, src/core/*, src/config/*, tests/*
2. Проанализирован src/main.js (12 128 строк): функции, системы, техдолг, архитектура
3. Проанализированы все 14 docs/project файлов
4. Проанализированы core модули (asset_loader, save_manager, unit_controller, storage_guard)
5. Проанализированы все 5 Playwright тестов
6. Написан comprehensive отчёт: docs/project/GLM_STRATEGY_REVIEW_20260510.md

Отчёт содержит 17 разделов:
1. Executive Summary
2. Current Project State (что реализовано / MVP / временное / техдолг)
3. Map Generation Ideas (8 идей с оценками)
4. Unit Animation and Visual Feedback Ideas (7 подкатегорий)
5. Economy Ideas (quick wins / medium-term / late-game)
6. Factions and Asymmetry (4 фракции + random)
7. Combat System Ideas (MVP / medium / defense tower / bomber)
8. Enemy Bot / AI Ideas (scripted / rule-based / advanced / fog policy)
9. Gameplay Features Missing from Roadmap (13 идей)
10. UI / UX Ideas (8 подкатегорий)
11. Architecture and main.js Refactor (4 фазы extraction, 10 безопасных PR)
12. Engine / Platform Future (8 вариантов с оценками)
13. Online / Multiplayer Possibilities (7 вариантов)
14. Testing / QA / Tooling (8 идей + PR/regression checklist)
15. Recommended Roadmap (1-2 недели / 1 месяц / later)
16. Top 10 Recommendations (с приоритетами и рисками)
17. What NOT to Do Yet (10 пунктов)

Изменённые файлы:
- docs/project/GLM_STRATEGY_REVIEW_20260510.md (new)
- PATCH_REPORT.txt (добавлена эта запись)

Проверки:
- node --check src/main.js -> PASS (smoke only, без изменений)
- git diff --name-only: только docs/project/GLM_STRATEGY_REVIEW_20260510.md + PATCH_REPORT.txt

Безопасность:
- src/* не изменён
- assets/* не изменён
- index.html не изменён
- gameplay не изменён
- Google Drive sync не тронут
- Production paths не затронуты

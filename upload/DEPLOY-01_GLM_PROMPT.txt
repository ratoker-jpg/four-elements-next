TASK: DEPLOY-01 — Configure GitHub Pages deployment for four-elements-next

Repo: ratoker-jpg/four-elements-next
Base branch: main

Context:
NEXT-02 is merged. The app works in repo, but public URL
https://ratoker-jpg.github.io/four-elements-next/
does not work yet because GitHub Pages deployment is not configured.

Goal:
Create a small deployment PR.

Scope:
1. Add Vite base for GitHub Pages:
   - Use '/four-elements-next/' only for GitHub Pages builds.
   - Keep local dev/preview working at '/'.
   - Suggested:
     const isGitHubPages = process.env.GITHUB_PAGES === 'true';
     base: isGitHubPages ? '/four-elements-next/' : '/'

2. Fix asset manifest paths if needed:
   - Current manifest uses '/assets/...'.
   - On GitHub Pages project site, assets must resolve under /four-elements-next/.
   - Recommended: use import.meta.env.BASE_URL or a helper:
     const assetPath = (path: string) => `${import.meta.env.BASE_URL}${path.replace(/^\//, '')}`;
   - Ensure local dev still works.

3. Add GitHub Actions workflow:
   .github/workflows/deploy-pages.yml

Workflow requirements:
- Runs on push to main and workflow_dispatch.
- Uses Node 22.
- npm ci.
- npm run type-check.
- npm run test.
- Build with:
  GITHUB_PAGES=true npm run build
- Upload dist with actions/upload-pages-artifact.
- Deploy with actions/deploy-pages.
- permissions:
  contents: read
  pages: write
  id-token: write

4. Update README with public URL and deployment note.

Out of scope:
- no gameplay changes;
- no map/render/economy changes;
- no asset changes;
- no NEXT-03 work.

Checks:
- npm run type-check
- npm run build
- npm run test
- npm run test:e2e if possible
- Verify dist/index.html references /four-elements-next/assets/... or correct base-relative paths.

Return CODE_SUMMARY:
- Branch
- PR URL
- Files changed
- Exact changes
- Checks
- Any manual repo settings required:
  GitHub repo Settings -> Pages -> Source: GitHub Actions
